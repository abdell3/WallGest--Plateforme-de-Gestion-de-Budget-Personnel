<div class="flex min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
  <aside class="w-72 bg-white/90 backdrop-blur-sm border-r border-gray-200 shadow-lg">
    <div class="p-6 border-b border-gray-100">
      <div class="flex items-center gap-3 mb-6">
        <div class="h-12 w-12 rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 text-white grid place-items-center font-bold text-lg">W</div>
        <div>
          <h2 class="text-xl font-bold text-gray-900">Mon Portefeuille</h2>
          <p class="text-sm text-gray-500">Tableau de bord</p>
        </div>
      </div>
    </div>
    
    <nav class="p-4 space-y-2">
      <a href="/dashboard" class="flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all <%= currentView === 'overview' ? 'bg-gradient-to-r from-blue-500 to-purple-600 text-white shadow-lg' : 'text-gray-600 hover:text-gray-900 hover:bg-gray-100' %>">
        <svg class="mr-3 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
        </svg>
        Vue d'ensemble
      </a>
      
      <a href="/saving-goals" class="flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all text-gray-600 hover:text-gray-900 hover:bg-gray-100">
        <svg class="mr-3 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.592 1L21 6m-2 6h2m-8 6h2m-6 4H5a2 2 0 01-2-2V7a2 2 0 012-2h14a2 2 0 012 2v10a2 2 0 01-2 2h-2M5 7h14"></path>
        </svg>
        Objectifs d'épargne
      </a>
      
      <div class="mt-8">
        <h3 class="px-4 text-xs font-semibold text-gray-500 uppercase tracking-wider mb-3">Mes Catégories</h3>
        <div class="space-y-1">
          <% if (categories && categories.length) { %>
            <% categories.forEach(category => { %>
              <a href="/dashboard/category/<%= category.id %>" class="flex items-center justify-between px-4 py-3 text-sm font-medium rounded-xl transition-all <%= (typeof currentCategory !== 'undefined' && currentCategory && currentCategory.id === category.id) ? 'bg-gradient-to-r from-blue-500 to-purple-600 text-white shadow-lg' : 'text-gray-600 hover:text-gray-900 hover:bg-gray-100' %>">
                <div class="flex items-center">
                  <div class="w-3 h-3 rounded-full bg-gradient-to-r from-blue-400 to-purple-500 mr-3"></div>
                  <span><%= category.title %></span>
                </div>
                <span class="text-xs <%= (typeof currentCategory !== 'undefined' && currentCategory && currentCategory.id === category.id) ? 'text-blue-100' : 'text-gray-400' %>"><%= category.budget %> MAD</span>
              </a>
            <% }) %>
          <% } %>
          
          <div class="px-4 py-3">
            <button onclick="document.getElementById('requestModal').classList.remove('hidden')" class="w-full flex items-center justify-center px-4 py-3 bg-gradient-to-r from-emerald-500 to-emerald-600 text-white text-sm font-medium rounded-xl hover:from-emerald-600 hover:to-emerald-700 transition-all transform hover:scale-105 shadow-lg">
              <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
              </svg>
              Demander une catégorie
            </button>
          </div>
        </div>
      </div>
      
      <div class="pt-4 mt-4 border-t border-gray-200">
        <a href="/auth/logout" class="flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all text-red-600 hover:text-red-700 hover:bg-red-50">
          <svg class="mr-3 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
          </svg>
          Déconnexion
        </a>
      </div>
    </nav>
  </aside>

  <main class="flex-1 overflow-auto">
    <div class="p-8">
      <% if (currentView === 'overview') { %>
        <div class="mb-8">
          <h1 class="text-3xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent mb-2">Vue d'ensemble</h1>
          <p class="text-gray-600">Statistiques globales de tous vos portefeuilles</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
          <div class="bg-white/80 backdrop-blur-sm rounded-2xl p-6 shadow-xl border border-white/20">
            <div class="flex items-center">
              <div class="flex-shrink-0 bg-gradient-to-r from-blue-500 to-blue-600 text-white rounded-2xl p-4">
                <svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.592 1L21 6m-2 6h2m-8 6h2m-6 4H5a2 2 0 01-2-2V7a2 2 0 012-2h14a2 2 0 012 2v10a2 2 0 01-2 2h-2M5 7h14"></path>
                </svg>
              </div>
              <div class="ml-6">
                <p class="text-sm font-medium text-gray-500 mb-1">Solde Total</p>
                <p class="text-3xl font-bold text-gray-900"><%= totalBalance %> <span class="text-lg text-gray-500">MAD</span></p>
              </div>
            </div>
          </div>
          
          <div class="bg-white/80 backdrop-blur-sm rounded-2xl p-6 shadow-xl border border-white/20">
            <div class="flex items-center">
              <div class="flex-shrink-0 bg-gradient-to-r from-emerald-500 to-emerald-600 text-white rounded-2xl p-4">
                <svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8L11 17m-5 2a2 2 0 11-4 0 2 2 0 014 0zM12 18a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
              </div>
              <div class="ml-6">
                <p class="text-sm font-medium text-gray-500 mb-1">Dépôts Totaux</p>
                <p class="text-3xl font-bold text-emerald-600">+<%= totalDeposits %> <span class="text-lg text-emerald-400">MAD</span></p>
              </div>
            </div>
          </div>
          
          <div class="bg-white/80 backdrop-blur-sm rounded-2xl p-6 shadow-xl border border-white/20">
            <div class="flex items-center">
              <div class="flex-shrink-0 bg-gradient-to-r from-red-500 to-red-600 text-white rounded-2xl p-4">
                <svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 17h8m0 0V9m0 8l-8-8-4 4-6-6"></path>
                </svg>
              </div>
              <div class="ml-6">
                <p class="text-sm font-medium text-gray-500 mb-1">Retraits Totaux</p>
                <p class="text-3xl font-bold text-red-600">-<%= totalWithdraws %> <span class="text-lg text-red-400">MAD</span></p>
              </div>
            </div>
          </div>
        </div>

        <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-8">
          <h2 class="text-2xl font-bold text-gray-900 mb-6">Mes Catégories</h2>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (wallets && wallets.length) { %>
              <% wallets.forEach(wallet => { %>
                <div class="bg-gradient-to-br from-gray-50 to-gray-100 rounded-xl p-6 border border-gray-200 hover:shadow-lg transition-all transform hover:scale-105">
                  <div class="flex items-center justify-between mb-4">
                    <h3 class="font-bold text-lg text-gray-900"><%= wallet.Category ? wallet.Category.title : 'Catégorie inconnue' %></h3>
                    <div class="w-4 h-4 rounded-full bg-gradient-to-r from-blue-400 to-purple-500"></div>
                  </div>
                  <div class="space-y-2 mb-4">
                    <p class="text-sm text-gray-600">Budget: <span class="font-semibold text-gray-900"><%= wallet.Category ? wallet.Category.budget : 0 %> MAD</span></p>
                    <p class="text-sm text-gray-600">Solde: <span class="font-semibold <%= wallet.amountG >= 0 ? 'text-emerald-600' : 'text-red-600' %>"><%= wallet.amountG %> MAD</span></p>
                  </div>
                  <a href="/dashboard/category/<%= wallet.categoryId %>" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-blue-500 to-purple-600 text-white text-sm font-medium rounded-lg hover:from-blue-600 hover:to-purple-700 transition-all">
                    Gérer
                    <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                  </a>
                </div>
              <% }) %>
            <% } else { %>
              <div class="col-span-full text-center py-12">
                <div class="w-16 h-16 bg-gray-200 rounded-full flex items-center justify-center mx-auto mb-4">
                  <svg class="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 7a2 2 0 002-2h10a2 2 0 002 2v2M7 7h10"></path>
                  </svg>
                </div>
                <p class="text-gray-500 text-lg">Aucun portefeuille créé</p>
                <p class="text-gray-400">Sélectionnez une catégorie pour commencer</p>
              </div>
            <% } %>
          </div>
        </div>

        <% if (savingGoals && savingGoals.length > 0) { %>
          <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-900 mb-6">Objectifs d'épargne actifs</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              <% savingGoals.forEach(goal => { %>
                <div class="bg-white/80 backdrop-blur-sm rounded-2xl p-6 shadow-xl border border-white/20 hover:shadow-2xl transition-all duration-300">
                  <div class="flex items-start justify-between mb-4">
                    <div>
                      <h3 class="text-lg font-semibold text-gray-900"><%= goal.title %></h3>
                      <p class="text-sm text-gray-500"><%= goal.Category.title %></p>
                    </div>
                    <span class="px-2 py-1 text-xs font-medium rounded-full bg-gradient-to-r from-blue-500 to-purple-600 text-white">
                      <%= goal.priority %>
                    </span>
                  </div>
                  
                  <div class="mb-4">
                    <div class="flex justify-between text-sm text-gray-600 mb-2">
                      <span>Progrès</span>
                      <span><%= goal.currentAmount %> / <%= goal.targetAmount %> MAD</span>
                    </div>
                    <div class="w-full bg-gray-200 rounded-full h-2">
                      <div class="bg-gradient-to-r from-emerald-500 to-emerald-600 h-2 rounded-full" style="width: <%= Math.min((goal.currentAmount / goal.targetAmount * 100), 100).toFixed(1) %>%"></div>
                    </div>
                    <p class="text-xs text-gray-500 mt-1"><%= (goal.currentAmount / goal.targetAmount * 100).toFixed(1) %>% complété</p>
                  </div>
                  
                  <% if (goal.deadline) { %>
                    <p class="text-sm text-gray-600">
                      <svg class="w-4 h-4 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                      </svg>
                      Échéance: <%= new Date(goal.deadline).toLocaleDateString('fr-FR') %>
                    </p>
                  <% } %>
                </div>
              <% }) %>
            </div>
          </div>
        <% } %>

      <% } else if (currentView === 'category') { %>
        <div class="mb-8">
          <div class="flex items-center gap-4 mb-4">
            <a href="/dashboard" class="text-gray-500 hover:text-gray-700">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
              </svg>
            </a>
            <h1 class="text-3xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent">Catégorie: <%= currentCategory.title %></h1>
          </div>
          <p class="text-gray-600">Gérez votre budget et vos transactions pour cette catégorie</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-8">
          <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-6">
            <h3 class="text-xl font-bold text-gray-900 mb-6 flex items-center">
              <svg class="w-6 h-6 mr-2 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 7a2 2 0 002-2h10a2 2 0 002 2v2M7 7h10"></path>
              </svg>
              Détails du Portefeuille
            </h3>
            <div class="space-y-4">
              <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                <span class="text-gray-600">Budget Mensuel</span>
                <span class="font-bold text-gray-900"><%= currentCategory.budget %> MAD</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                <span class="text-gray-600">Solde Actuel</span>
                <span class="font-bold <%= currentWallet.amountG >= 0 ? 'text-emerald-600' : 'text-red-600' %>"><%= currentWallet.amountG %> MAD</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                <span class="text-gray-600">Dépôts</span>
                <span class="font-bold text-emerald-600">+<%= currentWallet.totalDepo %> MAD</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                <span class="text-gray-600">Retraits</span>
                <span class="font-bold text-red-600">-<%= currentWallet.totalWithdraw %> MAD</span>
              </div>
              <% 
                const maxWithdraw = Math.floor(currentCategory.budget * 0.65);
                const remainingWithdraw = maxWithdraw - (currentWallet.totalWithdraw || 0);
              %>
              <div class="flex justify-between items-center p-3 bg-blue-50 rounded-lg border border-blue-200">
                <span class="text-blue-700">Retrait Max (65%)</span>
                <span class="font-bold text-blue-900"><%= maxWithdraw %> MAD</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-blue-50 rounded-lg border border-blue-200">
                <span class="text-blue-700">Retrait Restant</span>
                <span class="font-bold <%= remainingWithdraw >= 0 ? 'text-emerald-600' : 'text-red-600' %>"><%= remainingWithdraw %> MAD</span>
              </div>
            </div>
          </div>

          <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-6">
            <h3 class="text-xl font-bold text-gray-900 mb-6 flex items-center">
              <svg class="w-6 h-6 mr-2 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
              Mettre à jour le Budget
            </h3>
            <form action="/dashboard/budget" method="POST" class="space-y-4">
              <input type="hidden" name="categoryId" value="<%= currentCategory.id %>">
              <div>
                <label for="budget" class="block text-sm font-medium text-gray-700 mb-2">Nouveau Budget Mensuel</label>
                <input type="number" id="budget" name="budget" value="<%= currentCategory.budget %>" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all" required>
              </div>
              <button type="submit" class="w-full bg-gradient-to-r from-purple-600 to-purple-700 text-white py-3 px-6 rounded-xl hover:from-purple-700 hover:to-purple-800 transition-all transform hover:scale-105 font-medium">
                Mettre à jour le Budget
              </button>
            </form>
          </div>
        </div>

        <% if (savingGoals && savingGoals.length > 0) { %>
          <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-6 mb-8">
            <h3 class="text-xl font-bold text-gray-900 mb-6 flex items-center">
              <svg class="w-6 h-6 mr-2 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.592 1L21 6m-2 6h2m-8 6h2m-6 4H5a2 2 0 01-2-2V7a2 2 0 012-2h14a2 2 0 012 2v10a2 2 0 01-2 2h-2M5 7h14"></path>
              </svg>
              Objectifs d'épargne pour cette catégorie
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <% savingGoals.forEach(goal => { %>
                <div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-xl p-4 border border-purple-200">
                  <div class="flex items-start justify-between mb-3">
                    <div>
                      <h4 class="font-semibold text-gray-900"><%= goal.title %></h4>
                      <p class="text-sm text-gray-600">Objectif: <%= goal.targetAmount %> MAD</p>
                    </div>
                    <span class="px-2 py-1 text-xs font-medium rounded-full bg-purple-200 text-purple-800">
                      <%= goal.priority %>
                    </span>
                  </div>
                  
                  <div class="mb-3">
                    <div class="flex justify-between text-sm text-gray-600 mb-1">
                      <span>Progrès</span>
                      <span><%= goal.currentAmount %> MAD</span>
                    </div>
                    <div class="w-full bg-gray-200 rounded-full h-2">
                      <div class="bg-gradient-to-r from-purple-500 to-purple-600 h-2 rounded-full" style="width: <%= Math.min((goal.currentAmount / goal.targetAmount * 100), 100).toFixed(1) %>%"></div>
                    </div>
                    <p class="text-xs text-gray-500 mt-1"><%= (goal.currentAmount / goal.targetAmount * 100).toFixed(1) %>% complété</p>
                  </div>
                  
                  <% if (goal.deadline) { %>
                    <p class="text-xs text-gray-600">
                      <svg class="w-3 h-3 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                      </svg>
                      Échéance: <%= new Date(goal.deadline).toLocaleDateString('fr-FR') %>
                    </p>
                  <% } %>
                </div>
              <% }) %>
            </div>
            <div class="mt-4">
              <a href="/saving-goals" class="inline-flex items-center text-purple-600 hover:text-purple-700 font-medium">
                Gérer tous les objectifs
                <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>
        <% } %>

        <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-6 mb-8">
          <h3 class="text-xl font-bold text-gray-900 mb-6 flex items-center">
            <svg class="w-6 h-6 mr-2 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            Ajouter une Transaction
          </h3>
          <% if (typeof req !== 'undefined' && req.query && req.query.error) { %>
            <div class="mb-4 p-4 bg-red-100 border border-red-300 text-red-700 rounded-xl">
              <%= req.query.error %>
            </div>
          <% } %>
          <% if (typeof req !== 'undefined' && req.query && req.query.success) { %>
            <div class="mb-4 p-4 bg-green-100 border border-green-300 text-green-700 rounded-xl">
              <%= req.query.success %>
            </div>
          <% } %>
          <form action="/dashboard/transaction" method="POST" class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <input type="hidden" name="walletId" value="<%= currentWallet ? currentWallet.id : '' %>">
            <div>
              <label for="reference" class="block text-sm font-medium text-gray-700 mb-2">Référence</label>
              <input type="text" id="reference" name="reference" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all" placeholder="Ex: Achat supermarché" required>
            </div>
            <div>
              <label for="amount" class="block text-sm font-medium text-gray-700 mb-2">Montant</label>
              <input type="number" id="amount" name="amount" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all" placeholder="0" required min="1">
            </div>
            <div>
              <label for="type" class="block text-sm font-medium text-gray-700 mb-2">Type</label>
              <select id="type" name="type" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all">
                <option value="deposit">💰 Dépôt</option>
                <option value="withdraw">💸 Retrait</option>
              </select>
            </div>
            <div class="flex items-end">
              <button type="submit" class="w-full bg-gradient-to-r from-green-600 to-green-700 text-white py-3 px-6 rounded-xl hover:from-green-700 hover:to-green-800 transition-all transform hover:scale-105 font-medium">
                Ajouter
              </button>
            </div>
          </form>
        </div>

        <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl border border-white/20 p-6">
          <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
            <svg class="w-6 h-6 mr-2 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
            </svg>
            Historique des Transactions
          </h2>
          <% if (transactions && transactions.length) { %>
            <div class="space-y-3">
              <% transactions.forEach(t => { %>
                <div class="flex justify-between items-center p-4 bg-gray-50 rounded-xl border border-gray-200 hover:shadow-md transition-all">
                  <div class="flex items-center">
                    <div class="w-12 h-12 rounded-xl <%= t.deposit > 0 ? 'bg-emerald-100 text-emerald-600' : 'bg-red-100 text-red-600' %> flex items-center justify-center mr-4">
                      <% if (t.deposit > 0) { %>
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                        </svg>
                      <% } else { %>
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 12H4"></path>
                        </svg>
                      <% } %>
                    </div>
                    <div>
                      <p class="font-semibold text-gray-900"><%= t.reference %></p>
                      <p class="text-sm text-gray-500"><%= new Date(t.createdAt).toLocaleDateString('fr-FR', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></p>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-xl font-bold <%= t.deposit > 0 ? 'text-emerald-600' : 'text-red-600' %>">
                      <%= t.deposit > 0 ? '+' : '-' %><%= t.deposit > 0 ? t.deposit : t.withdraw %> MAD
                    </div>
                    <div class="text-sm text-gray-500">
                      <%= t.deposit > 0 ? 'Dépôt' : 'Retrait' %>
                    </div>
                  </div>
                </div>
              <% }) %>
            </div>
          <% } else { %>
            <div class="text-center py-12">
              <div class="w-16 h-16 bg-gray-200 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
                </svg>
              </div>
              <p class="text-gray-500 text-lg">Aucune transaction</p>
              <p class="text-gray-400">Commencez par ajouter votre première transaction</p>
            </div>
          <% } %>
        </div>
      <% } %>
    </div>
  </main>
</div>

<div id="requestModal" class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm z-50">
  <div class="flex items-center justify-center min-h-screen">
  <div class="bg-white rounded-2xl p-8 w-96 max-w-md mx-4 shadow-2xl">
    <h3 class="text-2xl font-bold text-gray-900 mb-6">Demander une nouvelle catégorie</h3>
    <form action="/dashboard/category-request" method="POST" class="space-y-6">
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-2">Nom de la catégorie</label>
        <input type="text" name="title" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all" placeholder="Ex: Voyage, Sport..." required>
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-2">Raison de la demande</label>
        <textarea name="reason" rows="4" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all" placeholder="Expliquez pourquoi vous avez besoin de cette catégorie..." required></textarea>
      </div>
      <div class="flex gap-3">
        <button type="submit" class="flex-1 bg-gradient-to-r from-blue-600 to-purple-600 text-white py-3 px-6 rounded-xl hover:from-blue-700 hover:to-purple-700 transition-all font-medium">
          Envoyer la demande
        </button>
        <button type="button" onclick="document.getElementById('requestModal').classList.add('hidden')" class="flex-1 bg-gray-200 text-gray-700 py-3 px-6 rounded-xl hover:bg-gray-300 transition-all font-medium">
          Annuler
        </button>
      </div>
    </form>
  </div>
  </div>
</div>

<script>
  setTimeout(() => {
    const flash = document.querySelector('.flash-message');
    if (flash) flash.remove();
  }, 5000);

</script>
</body>
</html>
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Connexion</title>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
  <div class="bg-white p-6 rounded shadow-md w-96">
    <h1 class="text-2xl font-bold mb-4">Se connecter</h1>
    <% if (error) { %>
      <p class="text-red-500 mb-3"><%= error %></p>
    <% } %>
    <form method="POST" action="/auth/login" class="space-y-3">
      <input type="email" name="email" placeholder="Email" class="w-full p-2 border rounded" required>
      <input type="password" name="password" placeholder="Mot de passe" class="w-full p-2 border rounded" required>
      <button type="submit" class="w-full bg-green-500 text-white p-2 rounded">Connexion</button>
    </form>
    <p class="mt-3 text-sm">Pas encore de compte ? <a href="/auth/register" class="text-blue-500">S'inscrire</a></p>
  </div>
</body>
</html>
